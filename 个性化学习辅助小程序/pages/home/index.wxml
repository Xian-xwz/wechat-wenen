<!-- 学习概况数据大屏 -->
<view class="dashboard-container">
  <!-- 顶部导航 -->
  <view class="dashboard-header">
    <text class="header-title">学习看板</text>
    <t-icon name="refresh" size="48rpx" bindtap="onRefresh" class="refresh-icon {{isRefreshing ? 'rotating' : ''}}" />
  </view>

  <!-- 滚动内容区 -->
  <scroll-view class="dashboard-scroll" scroll-y enable-flex>
    <!-- 核心指标卡片区 -->
    <view class="stats-cards">
      <view class="stats-card stats-card--primary">
        <view class="stats-icon">📚</view>
        <view class="stats-content">
          <text class="stats-value">{{learningStats.totalHours || 0}}</text>
          <text class="stats-label">累计学习(小时)</text>
        </view>
      </view>
      <view class="stats-card stats-card--success">
        <view class="stats-icon">✅</view>
        <view class="stats-content">
          <text class="stats-value">{{learningStats.todayQuestions || 0}}</text>
          <text class="stats-label">今日刷题</text>
        </view>
      </view>
      <view class="stats-card stats-card--warning">
        <view class="stats-icon">🎯</view>
        <view class="stats-content">
          <text class="stats-value">{{learningStats.accuracy || 0}}%</text>
          <text class="stats-label">平均正确率</text>
        </view>
      </view>
      <view class="stats-card stats-card--danger">
        <view class="stats-icon">🔥</view>
        <view class="stats-content">
          <text class="stats-value">{{learningStats.streakDays || 0}}</text>
          <text class="stats-label">连续打卡(天)</text>
        </view>
      </view>
    </view>

    <!-- 快速入口卡片 -->
    <view class="quick-actions">
      <view class="action-card" bindtap="goToQuiz">
        <view class="action-icon action-icon--quiz">📝</view>
        <text class="action-label">开始刷题</text>
      </view>
      <view class="action-card" bindtap="goToWrongBook">
        <view class="action-icon action-icon--wrong">📖</view>
        <text class="action-label">错题本</text>
      </view>
      <view class="action-card" bindtap="goToLearningPath">
        <view class="action-icon action-icon--path">🗺️</view>
        <text class="action-label">学习路径</text>
      </view>
      <view class="action-card" bindtap="goToRecommend">
        <view class="action-icon action-icon--recommend">💡</view>
        <text class="action-label">个性推荐</text>
      </view>
    </view>

    <!-- 知识雷达图 -->
    <view class="chart-section">
      <view class="section-header">
        <text class="section-title">知识掌握度</text>
        <text class="section-subtitle">各知识点能力评估</text>
      </view>
      <view class="chart-container radar-chart">
        <ec-canvas id="radar-chart" canvas-id="radar-chart" ec="{{radarEc}}"></ec-canvas>
      </view>
    </view>

    <!-- 趋势折线图 -->
    <view class="chart-section">
      <view class="section-header">
        <text class="section-title">学习趋势</text>
        <text class="section-subtitle">近7日活跃度与正确率</text>
      </view>
      <view class="chart-container trend-chart">
        <ec-canvas id="trend-chart" canvas-id="trend-chart" ec="{{trendEc}}"></ec-canvas>
      </view>
    </view>

    <!-- 推荐任务栏 -->
    <view class="tasks-section">
      <view class="section-header">
        <text class="section-title">今日必做</text>
        <text class="view-all" bindtap="viewAllTasks">查看全部</text>
      </view>
      <view class="task-list">
        <view 
          wx:for="{{dailyTasks}}" 
          wx:key="id"
          class="task-item {{item.completed ? 'task-item--completed' : ''}}"
          bindtap="toggleTask"
          data-id="{{item.id}}"
        >
          <view class="task-checkbox">
            <t-icon name="{{item.completed ? 'check-circle-filled' : 'circle'}}" 
                    size="40rpx" 
                    color="{{item.completed ? '#52C41A' : '#D9D9D9'}}" />
          </view>
          <view class="task-content">
            <text class="task-title">{{item.title}}</text>
            <text class="task-desc">{{item.desc}}</text>
          </view>
          <view class="task-reward">
            <text class="reward-text">+{{item.points}}积分</text>
          </view>
        </view>
      </view>
    </view>

    <!-- 底部留白（适配 tabBar） -->
    <view class="bottom-spacer"></view>
  </scroll-view>
</view>

<t-message id="t-message" />
